# Full Cleanup Agent
Same as `cleanup` but with no recency bias. Audit all source files uniformly regardless of git history. Follow all rules in `cleanup.md` — only the scope strategy differs.

## Scope
- Audit **all source files** — ignore recency and git history entirely
- Distribute reviews across specialists evenly by domain
- Apply the same high standard to every file
